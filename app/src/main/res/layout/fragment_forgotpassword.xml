<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:android="http://schemas.android.com/apk/res/android"
>

    <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center_vertical"
    >

        <EditText
                android:id="@+id/mobilenumber"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_margin="@dimen/forgot_password_margin"
                android:hint="@string/login_hint"
                android:inputType="number"
                android:maxLength="10"
                android:singleLine="true"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintRight_toRightOf="parent" tools:ignore="Autofill"/>

        <Button
                android:id="@+id/login"
                android:layout_width="@dimen/login_btn_width"
                android:layout_height="wrap_content"
                android:layout_margin="@dimen/forgot_password_margin"
                android:background="@drawable/loginbutton_shape"
                android:text="@string/find_your_account_btn_text"
                android:textColor="@color/white"
                android:textSize="@dimen/activity_button_size"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@+id/mobilenumber"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>